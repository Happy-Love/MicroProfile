version: '3'
services:

  db:
    image: postgres:12.2
    #volumes:
    #  - ./tmp/db:/var/lib/postgresql/data
    ports:
      - "5435:5432"  
    environment: 
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: "db"

  web:
    image:
      yuraprogrammer/microinstagram:0.0.2
    #build: .
    command: bash -c "rm -f tmp/pids/server.pid && yarn install --check-files && bundle exec rails s -p 3000 -b '0.0.0.0'"
    volumes:
      - .:/myapp
    ports:
      - "3000:3000"
    environment:
      EMAIL_PASSWORD: "Uruu2013+2013"
      EMAIL_LOGIN: "happylove.programmer"
      AWS_ACCESS_KEY_ID: AKIAVAZLZLNCCS6NFCHV
      AWS_SECRET_ACCESS_KEY: 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
      AWS_BUCKET: railsas
      POSTGRES_USERNAME: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_HOST: db  
    depends_on:
      - db